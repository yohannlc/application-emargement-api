nelmio_api_doc:
    documentation:
        info:
            title: Application d'émargement électronique
            description: Une API qui permet la création d'une application d'émargement électronique
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
